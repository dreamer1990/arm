#!/bin/bash

#数组的复制

a=(100 200 300 AAA　BBB CCC)
echo "${a[*]}"
#数组中元素的个数
echo "${#a[@]}"


#将${a[*]} 当作一个字符串给b[0]
b=${a[*]}
echo "${b[*]}"
#数组中元素的个数
echo "${#b[*]}"

#注意与c=${a[*]}的区别
c=(${a[*]})
echo "${c[*]}"
echo "${#c[*]}"






exit 0
